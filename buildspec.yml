version: 1
frontend:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
    artifacts:
      files:
        - "**/*"
      baseDirectory: .next
  cache:
    paths:
      - node_modules/**/*
